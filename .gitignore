# OS
.DS_Store
Thumbs.db

# Python bytecode cache
__pycache__/
*.pyc

# Large local-only data / media
datasets/
dataset_extraction/
tests_videos/

# Pipeline outputs (pseudo-labels, hard-example curation, training runs)
outputs/
dataset_curation/

# Archived/shelved dev scripts (dataset, upload, training, grids, scratch) — local only
tools/_archive/

# Shelved lane-classifier experiments — local only
line_crossing/experiments/
line_crossing/lab_classifiers_v3.py

# Local Claude allow-rules (machine-specific paths) — not shared
.claude/settings.json

# Python processing unit outputs
output.mp4
notes.txt
test_1_world.jsonl
test_1_yolo.jsonl

# YOLOv8 model weights
yolov8m.pt
yolov8x.pt
yolov8n.pt
yolov8n-seg.pt

# DL lane-detection env (Phase 0) — never commit envs / build artifacts / cloned framework
.venv/
venv/
env/
roadguard-dl/
UnLanedet/
weights/
models/
*.so
*.pyd
build/
*.egg-info/
.eggs/

# Downloaded dataset videos (use tests_videos/download_videos.py to get them)
tests_videos/raw_videos/
test_videos/

# IDE
.idea/
