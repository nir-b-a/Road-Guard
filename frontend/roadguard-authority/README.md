# RoadGuard Authority Web App
## Setup
```bash
npm install
npm run dev
```
Make sure the backend is running on http://localhost:5000 first.
Register an authority account via the backend, then log in here.